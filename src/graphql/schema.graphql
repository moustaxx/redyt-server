type Query {
	info: String!
	showPosts: [Post!]!
}

type Mutation {
	addPost(title: String!, description: String!, author: String!): Post!
}

type Post {
	id: ID!
	title: String!
	description: String!
	author: String!
	createdOn: String
}